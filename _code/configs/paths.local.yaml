# -*- coding: utf-8 -*-
# configs/paths.local.yaml

# -------------------------------------------------------------------------
# Base Paths
# -------------------------------------------------------------------------
base_path: "M:/CALife/CAShop - 구매대행/_code"

root: "M:/CALife/CAShop - 구매대행"

code_dir: "${root}/_code"
scripts_dir: "${code_dir}/scripts"
modules_dir: "{code_dir}/modules"
db_dir: "{code_dir}/data/db"
fonts_dir: "{code_dir}/data/fonts"

configs_dir: "{code_dir}/configs"

configs_loader_paths:
  excel: "${configs_dir}/config_loader_excel.yaml"
  firefox: "${configs_dir}/config_loader_firefox.yaml"
  crawl: "${configs_dir}/config_loader_crawl.yaml"
  image: "${configs_dir}/config_loader_image.yaml"
  ocr: "${configs_dir}/config_loader_ocr.yaml"
  overlay: "${configs_dir}/config_loader_overlay.yaml"


output_data_dir: "${root}/_data"
public_dir: "${root}/_public"